  Introduction : 'GAN-free methods for gradient inversion attack.'
  
  optim : 'GAN_free'    #optim method, ['GAN_based', 'GAN_free']
  cost_fn : 'sim_cmpr0'   #the type of loss function 
  set_seed : 1      # random seed. Following geiping et. al., we do not give random seed for GAN-free methods.
  indices : 'def'      #decide which part of gradients to be involved in the final gradients match loss.
  weights : 'equal'    #weight of every graident scalar's matching loss
  init : 'randn'     #how we initial the original latent code.
  init_text : 'randn'     #how we initial the original latent code of text embedding.
  model : 'FedCola_TXT'   #FL model ['FedCola_IMG', 'FedCola_TXT', 'FedCola_IMG_TXT']
  restarts : 5

  num_images : 1   # the number of images to reconstruct at a batch
  num_exp : 1     # the number of experiments
  target_id : 10 # the target image id + iterative incrementation of id
  target_id_increment : 1 # the incrementation of target image id
  lr : 0.1            #learning rate for Yin et al.
  total_variation : -1     #the coefficient of total variation 
  image_norm : 0.000001    #the coefficient of norm regularizer for Yin et al.
  group_lazy : 0.01    #choose if we use group lazy regularization for Yin et al.
  
  bn_stat : 0       #choose if we use bn statistic to regularizer 

  max_iterations : 1000    #Maximum number of iterations for reconstruction.

  use_saved_models : False  # whether to use saved models or not
  model_load_dir : '/home/ubuntu/mnt-disk/results/fedcola/exp_name'  # the directory to load saved models from

  gias_lr : 0.00001   #For biggan, we'd better choose smaller learning rate. 

  # For input data
  generative_model : ''
  gen_dataset : 'ImageNet64'    # ['ImageNet64', 'FFHQ64']
  dataset : 'Coco_txt'   # ['Coco', 'Coco_img', 'Coco_txt']     
  data_path : 'N/A'  # specify your dataset path

  #For output data
  exp_name : 'ex1_FedCola_txt'   #Same latent space search
  output_dir : '/home/ubuntu/mnt-disk/results/censor-fedcola'

  #Choice for GAN-free methods
  geiping : true
  yin : false

  train_epochs: 10
  our_num_tries : 10
  #Defense parameter
  defense_method : None
  defense_setting : 
      noise : None
      clipping : None
      compression : None
      representation : None
      orthogonal : null


  # The pre-trained StyleGAN checkpoint
  ckpt: None

  #LR pace for training
  lr_same_pace: false 
